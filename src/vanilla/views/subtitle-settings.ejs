<div
    id="<%= id %>"
    class="avp-subtitle-settings avp-settings avp-popin"
    role="dialog"
    aria-labelledby="avp-subtitle-settings-title"
>
    <div class="avp-header">
        <button
            class="avp-back-link"
            aria-label="<%= t('settings.backLabel') %> - <%= t('displaySettings.header') %>"
        >
            <%= renderIcon("back", t("settings.back")) %>
        </button>
        <span id="avp-subtitle-settings-title" role="header"><%= t("subtitleSettings.header") %></span>
    </div>

    <%= renderRadioGroup(id, "subtitle", subtitleRadioItems) %>
</div>
